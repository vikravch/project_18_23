import React from 'react';
import style from './signHeader.module.css'

const SingHeader = () => {
    return (
        <div className={style.wrapper}>
            <p className={style.sign_header_text}>3legant.</p>
        </div>
    );
};

export default SingHeader;